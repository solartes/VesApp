@model IEnumerable<VesApp.Domain.Donation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Text)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UrlDireccion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UrlImagen)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Text)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UrlDireccion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UrlImagen)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdDonation }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdDonation }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdDonation })
        </td>
    </tr>
}

</table>
